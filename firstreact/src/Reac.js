import './reac.css';
export const Table = (props) => {
    return (
        <h1>You clicked { props.clicked } times</h1>
    )
}
export const But = (props) => {
    return (
        <button >click me</button>
    )
}
// { for(let i = 0; i < 10; i ++) {

// }}