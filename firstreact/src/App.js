import logo from "./logo.svg";
import "./App.css";
import { Table, But } from "./Reac";
const myArray = [<Table />]
function App() {
  return (
    <div className="App">
      <Table />
      <But />
      {/* <h1>snake</h1>
      {myArray.map((value, index) => (  
        value
     ))}   */}
    </div>
  );
}

export default App;
